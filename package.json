{
  "name": "abap-claude-mirror",
  "displayName": "ABAP Claude Mirror",
  "description": "Mirrors the active ADT (abap://) document to a real file and focuses it, so Claude Code's file context can read it",
  "version": "0.0.3",
  "publisher": "abgunes",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/abgunes/ABAP-Claude-Mirror"
  },
  "homepage": "https://github.com/abgunes/ABAP-Claude-Mirror",
  "bugs": {
    "url": "https://github.com/abgunes/ABAP-Claude-Mirror/issues"
  },
  "engines": { "vscode": "^1.80.0" },
  "icon": "images/icon.png",
  "categories": ["Programming Languages", "Other"],
  "keywords": ["abap", "adt", "sap", "claude", "claude code", "mirror"],
  "activationEvents": ["onStartupFinished"],
  "main": "./extension.js",
  "contributes": {
    "configuration": {
      "title": "ABAP Claude Mirror",
      "properties": {
        "abapClaudeMirror.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Mirror active abap:// ADT documents to real files (and sync edits back) so Claude Code can read/edit them. Toggle off to fully disable, no reload needed."
        }
      }
    },
    "commands": [
      {
        "command": "abapClaudeMirror.openMirror",
        "title": "ABAP Claude Mirror - Open Mirror Object"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "abapClaudeMirror.openMirror",
          "when": "resourceScheme == abap",
          "group": "z_abapClaudeMirror@1"
        }
      ],
      "editor/title/context": [
        {
          "command": "abapClaudeMirror.openMirror",
          "when": "resourceScheme == abap",
          "group": "z_abapClaudeMirror@1"
        }
      ]
    },
    "languages": [
      {
        "id": "abap-mirror-view",
        "extensions": [".abapmirror"],
        "aliases": ["ABAP (mirror view)"],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "abap-mirror-view",
        "scopeName": "source.abapmirror",
        "path": "./syntaxes/abap-mirror.tmLanguage.json"
      }
    ]
  }
}
